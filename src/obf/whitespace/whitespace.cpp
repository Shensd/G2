#include "whitespace.hpp"

namespace obf {

    /**
     * Randomize the whitespace of a given string, converts spaces and newlines
     * into random mixes of both 
     * 
     * @param str string to randomize the whitespace of
     * @param intensity how intense to make the randomization
     * @return a string with much worse whitespace
     */
    std::string randomizeWhitespace(std::string str, int intesity) {

    }

}